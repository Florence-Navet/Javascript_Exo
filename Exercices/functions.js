/**
 * utiliser fonctions et methodes pour arondir un nb à virgule flottante
 * - arrondir nb reçu uniquement s'il est à virgule flottante et retourner un nb
 *
 * indice : *var ou Number(var) permet de convertir varibale en isNaN que nombre est bien un nombre
 * si nombre n'est pas un nombre, assignez lui "pas un nombre"
 * si nombre est un entier, ne modifiez pas sa valeur
 * ni nombre est nb à virgule flottante, arrondissez à 3 chiffre après la virgule et convertissez
 * le en nb
 * ex (5) -> 5
 * (5.555) -> 5.555
 * (3.333333333333) -> 3.333
 * (9.9999999999) -> 'Pas un nombre'
 * (undefined) -> 'Pas un nombre'
 * (null) -> 'Pas un nombre'
 * ('Bob') -> 'Pas un nombre'
 */
export function testFn(nombre) {
  // Ne touchez pas au return :
  return nombre;
}
